{
  "port": "env(PORT)",
  "settings": {
    "loggerFormat": "tiny",
    "session": {
      "store": "@foal/typeorm"
    },
    "disk": {
      "local": {
        "directory": "assets"
      }
    }
  },
  "database": {
    "type": "better-sqlite3",
    "database": "./db.sqlite3"
  }
}
